Project meeting - Nov 20th, 2017

1>edit ->(带着dealerID, vehicle ID)
	    -> new screen with previous information
2>new -> if click an existing record, with template
	    -> if not click an existin grecord, empty template
	    caution here:new button,remind user to click one record or not(template/ empty template)
2>delete-> new page (Y N )
	refresh after deletion(belong to manage inventory list screen)

UI
1>Vehicle Details @yuxin
2>Delete confirmation @yuanyuan
3>validation(when you step out, every field validate once),点击save的时候再validate一遍，每一个validation return boolean, when click button, check fields' validation results check when the VID is already exsting,warning appears @yangchun
4>edit and save when you want to change the VID, the previous one will be rewritten @niulu



confirm later
1>fields design : price button(show the original price)
2>refresh the inventory list screen after each action(add, edit, del)


Backlog:
1>hot key
